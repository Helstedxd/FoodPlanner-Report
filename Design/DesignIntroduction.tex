In this chapter, domains will be defined and described, before design requirements are set by interviews and described scenarios \fxnote{This is written from the order in which it appears in the report, but in Design Requirements it is mentioned that that given section will use scenarios but these are not introduced until after the section, how/when should what appear?}
. Next a section will define some user stories, which are used to construct conceptional scenarios, which in turn can be used to create some concrete scenarios, these concrete scenarios are then evaluated in order to describe a scenario corpus.\fxnote{Used for what?}